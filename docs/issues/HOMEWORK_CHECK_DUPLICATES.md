# Issue: Дубликаты проверок домашних заданий

## Статус
🔴 **Открыто** - Требуется исправление

## Приоритет
🔴 **Высокий** - Критическая проблема, влияющая на корректность подсчета прогресса проверки

## Описание проблемы

В системе обнаружены дубликаты проверок домашних заданий - для одной пары (lessonId, pupilId) создается несколько записей в таблице HomeworkCheck. Это приводит к неправильному подсчету прогресса проверки (например, 417% вместо максимум 100%).

### Симптомы

1. **Неправильный процент проверки**: На странице урока отображается процент проверки более 100% (например, 417%)
2. **Неправильный подсчет проверенных учеников**: Отображается "Проверено: 25 из 6" вместо "Проверено: 6 из 6"
3. **Дубликаты в базе данных**: Для одного урока и одного ученика существует несколько записей HomeworkCheck

### Пример проблемы

```
Урок: lesson-123
Ученик: pupil-456
Проверок в базе: 4 записи (должна быть 1)

Результат:
- Проверено: 25 из 6 (вместо 6 из 6)
- Процент: 417% (вместо 100%)
```

## Детальный анализ проблемы

### 1. Отсутствие проверки существования перед созданием

#### Техническая причина

**Текущая реализация создания проверок:**
- Функция `createHomeworkCheck` в `src/lib/db/mutations.ts` не проверяет существование проверки перед созданием
- Нет логики "upsert" (update or insert)
- Нет проверки уникальности пары (lessonId, pupilId) на уровне приложения

**Схема базы данных:**
- Таблица `HomeworkCheck` имеет GSI-1: `lessonId-pupilId-index` (PK: lessonId, SK: pupilId)
- В DynamoDB нет встроенного уникального ограничения на уровне базы данных
- Индекс `byLessonId` с `sortKeyFields: ["pupilId"]` не обеспечивает уникальность - это просто индекс для запросов

#### Пример проблемы в коде

```typescript
// src/lib/db/mutations.ts
export async function createHomeworkCheck(
  input: APITypes.CreateHomeworkCheckInput,
  condition?: APITypes.ModelHomeworkCheckConditionInput
): Promise<APITypes.HomeworkCheck> {
  // ❌ Нет проверки существования проверки для пары (lessonId, pupilId)
  const result = await amplifyData.create('HomeworkCheck', {
    input,
    condition,
  });
  return result as APITypes.HomeworkCheck;
}
```

### 2. Отсутствие уникального ограничения в DynamoDB

#### Техническая причина

**DynamoDB ограничения:**
- DynamoDB не поддерживает уникальные ограничения на уровне базы данных (как в реляционных БД)
- GSI (Global Secondary Index) не обеспечивает уникальность - это просто индексы для запросов
- Уникальность должна обеспечиваться на уровне приложения

**Текущая схема GraphQL:**
```graphql
type HomeworkCheck @model {
  id: ID!
  lessonId: ID! @index(name: "byLessonId", sortKeyFields: ["pupilId"])
  pupilId: ID! @index(name: "byPupilId", sortKeyFields: ["createdAt"])
  # ...
}
```

**Проблема:**
- Индекс `byLessonId` с `sortKeyFields: ["pupilId"]` создает GSI-1 с PK=lessonId и SK=pupilId
- Но это не предотвращает создание нескольких записей с одинаковыми lessonId и pupilId
- Каждая запись имеет уникальный `id` (PK), поэтому они могут существовать параллельно

### 3. Возможные сценарии создания дубликатов

1. **Множественные клики на кнопку сохранения**: Пользователь быстро нажимает кнопку несколько раз
2. **Race conditions**: Параллельные запросы на создание проверки для одного ученика
3. **Повторная проверка**: Пользователь начинает проверку заново, не проверив существующие данные
4. **Ошибки в логике массового создания**: При массовом создании проверок не проверяется существование

## Текущая реализация

### Цепочка вызовов

1. **Компонент проверки ДЗ** (если существует)
   - Вызывает Server Action для создания проверки

2. **Server Action** (если существует)
   - Вызывает `createHomeworkCheck` из `src/lib/db/mutations.ts`

3. **Data Access Layer**: `src/lib/db/mutations.ts`
   - `createHomeworkCheck()` - создает проверку без проверки существования
   - Нет функции для проверки существования проверки по паре (lessonId, pupilId)

4. **GraphQL Client**: `src/lib/db/amplify.ts`
   - `amplifyData.create()` - создает запись в DynamoDB

### Отсутствующие функции

1. **Функция проверки существования**: Нет функции `getHomeworkCheckByLessonAndPupil(lessonId, pupilId)`
2. **Функция upsert**: Нет функции `upsertHomeworkCheck()` для обновления или создания
3. **Валидация уникальности**: Нет проверки уникальности пары (lessonId, pupilId) перед созданием

## Варианты решения

### Вариант 1: Upsert Pattern (Рекомендуется)

**Подход**: Проверять существование проверки перед созданием. Если существует - обновлять, если нет - создавать.

**Преимущества:**
- ✅ Гарантирует уникальность пары (lessonId, pupilId)
- ✅ Позволяет обновлять существующие проверки
- ✅ Не требует изменений в схеме базы данных
- ✅ Простая реализация

**Недостатки:**
- ⚠️ Требует дополнительный запрос для проверки существования (1 дополнительный запрос на создание)

**Реализация:**
```typescript
// 1. Создать функцию проверки существования
export async function getHomeworkCheckByLessonAndPupil(
  lessonId: string,
  pupilId: string
): Promise<APITypes.HomeworkCheck | null> {
  // Использовать GSI-1: lessonId-pupilId-index
  // Query с lessonId = PK и pupilId = SK
}

// 2. Создать функцию upsert
export async function upsertHomeworkCheck(
  input: APITypes.CreateHomeworkCheckInput
): Promise<APITypes.HomeworkCheck> {
  // 1. Проверить существование через GSI-1
  const existing = await getHomeworkCheckByLessonAndPupil(
    input.lessonId,
    input.pupilId
  );
  
  // 2. Если существует - обновить
  if (existing) {
    return await updateHomeworkCheck({
      id: existing.id,
      ...input,
    });
  }
  
  // 3. Если не существует - создать
  return await createHomeworkCheck(input);
}
```

### Вариант 2: Условное создание с проверкой

**Подход**: Использовать `condition` в `createHomeworkCheck` для проверки отсутствия. Обрабатывать ошибку ConditionalCheckFailedException.

**Преимущества:**
- ✅ Меньше запросов (1 запрос вместо 2)
- ✅ Атомарная операция на уровне DynamoDB

**Недостатки:**
- ⚠️ Требует обработки ошибок ConditionalCheckFailedException
- ⚠️ Если проверка существует, нужно делать отдельный запрос для обновления
- ⚠️ Более сложная логика обработки ошибок

**Реализация:**
```typescript
export async function upsertHomeworkCheck(
  input: APITypes.CreateHomeworkCheckInput
): Promise<APITypes.HomeworkCheck> {
  try {
    // Попытка создать с условием отсутствия
    return await createHomeworkCheck(input, {
      // condition: проверка отсутствия через GSI-1
    });
  } catch (error) {
    if (error instanceof ConditionalCheckFailedException) {
      // Проверка существует, получить и обновить
      const existing = await getHomeworkCheckByLessonAndPupil(
        input.lessonId,
        input.pupilId
      );
      if (existing) {
        return await updateHomeworkCheck({
          id: existing.id,
          ...input,
        });
      }
    }
    throw error;
  }
}
```

### Вариант 3: Проверка на уровне Server Actions

**Подход**: Добавить проверку существования в Server Actions перед вызовом `createHomeworkCheck`.

**Преимущества:**
- ✅ Логика проверки на уровне бизнес-логики
- ✅ Можно добавить дополнительную валидацию

**Недостатки:**
- ⚠️ Дублирование логики в каждом Server Action
- ⚠️ Не решает проблему на уровне Data Access Layer

## Рекомендуемое решение

**Вариант 1: Upsert Pattern** - рекомендуется для реализации.

### Обоснование

1. **Простота реализации**: Не требует сложной обработки ошибок
2. **Надежность**: Гарантирует уникальность и правильное обновление данных
3. **Гибкость**: Позволяет обновлять существующие проверки без дополнительной логики
4. **Совместимость**: Не требует изменений в схеме базы данных

### План реализации

#### Шаг 1: Создать функцию проверки существования

**Файл**: `src/lib/db/queries.ts`

```typescript
/**
 * Get homework check by lesson ID and pupil ID
 * Uses GSI-1: lessonId-pupilId-index
 * @param lessonId - Lesson ID
 * @param pupilId - Pupil ID
 * @returns HomeworkCheck or null if not found
 */
export async function getHomeworkCheckByLessonAndPupil(
  lessonId: string,
  pupilId: string
): Promise<APITypes.HomeworkCheck | null> {
  // Использовать GraphQL query через GSI-1
  // Query с lessonId = PK и pupilId = SK
  // Вернуть первую найденную запись или null
}
```

#### Шаг 2: Создать функцию upsert

**Файл**: `src/lib/db/mutations.ts`

```typescript
/**
 * Upsert homework check (update if exists, create if not)
 * Ensures uniqueness of (lessonId, pupilId) pair
 * @param input - Homework check input data
 * @returns Created or updated HomeworkCheck
 */
export async function upsertHomeworkCheck(
  input: APITypes.CreateHomeworkCheckInput
): Promise<APITypes.HomeworkCheck> {
  // 1. Проверить существование через getHomeworkCheckByLessonAndPupil
  // 2. Если существует - обновить через updateHomeworkCheck
  // 3. Если не существует - создать через createHomeworkCheck
}
```

#### Шаг 3: Обновить все места создания проверок

**Файлы для обновления:**
- `src/actions/homework.ts` (если существует) - заменить `createHomeworkCheck` на `upsertHomeworkCheck`
- Компоненты проверки ДЗ (если существуют) - использовать `upsertHomeworkCheck`
- Server Actions для массового создания - использовать `upsertHomeworkCheck` для каждой проверки

#### Шаг 4: Очистка существующих дубликатов

**Скрипт**: `scripts/cleanup-homework-check-duplicates.ts`

```typescript
/**
 * Cleanup duplicate homework checks
 * For each (lessonId, pupilId) pair with duplicates:
 * - Keep the record with the latest updatedAt
 * - Delete other records
 */
async function cleanupDuplicates() {
  // 1. Получить все проверки из DynamoDB
  // 2. Группировать по (lessonId, pupilId)
  // 3. Для каждой пары с дубликатами:
  //    - Найти запись с самым поздним updatedAt
  //    - Удалить остальные записи
  // 4. Вывести отчет о проделанной работе
}
```

#### Шаг 5: Обновить логику подсчета прогресса

**Файл**: `src/app/(private)/lessons/[lessonId]/lesson-detail-content.tsx`

Уже исправлено:
- Фильтрация проверок только для активных учеников
- Удаление дубликатов по pupilId
- Ограничение процента 100%

## Связанные документы

### Архитектура и база данных
- [GraphQL Schema](../../database/GRAPHQL_SCHEMA.md) - схема HomeworkCheck
- [DynamoDB Schema](../../database/DYNAMODB_SCHEMA.md) - структура таблицы HomeworkChecks
- [ERD](../../database/ERD.md) - диаграмма сущностей

### Реализация
- [Phase 14: Проверка домашних заданий](../implementation/mvp/tasks/phase_14_homework.md) - задачи фазы
- [Server Actions](../../api/SERVER_ACTIONS.md) - API для проверок ДЗ
- [Validation](../../api/VALIDATION.md) - схемы валидации

### Код
- [src/lib/db/mutations.ts](../../../src/lib/db/mutations.ts) - функции создания проверок
- [src/lib/db/queries.ts](../../../src/lib/db/queries.ts) - функции запросов
- [src/app/(private)/lessons/[lessonId]/lesson-detail-content.tsx](../../../src/app/(private)/lessons/[lessonId]/lesson-detail-content.tsx) - страница урока

## Критерии успеха

1. ✅ Функция `upsertHomeworkCheck` создана и работает корректно
2. ✅ Все места создания проверок используют `upsertHomeworkCheck`
3. ✅ Существующие дубликаты очищены
4. ✅ Процент проверки не превышает 100%
5. ✅ Подсчет уникальных проверок работает корректно
6. ✅ Новые дубликаты не создаются

## Временная оценка

- Создание функции проверки существования: 1-2 часа
- Создание функции upsert: 1-2 часа
- Обновление всех мест создания проверок: 2-3 часа
- Создание скрипта очистки дубликатов: 2-3 часа
- Тестирование: 2-3 часа

**Итого**: 8-13 часов

---

**Дата создания**: 2 января 2026  
**Последнее обновление**: 2 января 2026

