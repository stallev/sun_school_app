# Seed Books Table via CLI - Инструкция

## Описание

Скрипт для заполнения таблицы Books всеми 66 книгами Библии через командную строку. Скрипт использует AWS CLI credentials для прямого доступа к DynamoDB, минуя AppSync API.

## Предварительные требования

1. ✅ **AWS CLI настроен** - credentials должны быть настроены через `aws configure`
2. ✅ **IAM permissions** - AWS credentials должны иметь права на DynamoDB (PutItem, Query, Scan на таблицу Book)
3. ✅ **DynamoDB таблица создана** - таблица Book должна быть создана через `amplify push`
4. ✅ **Зависимости установлены** - `npm install` должен быть выполнен
5. ✅ **tsx установлен** - для запуска TypeScript скриптов (`npm install -D tsx`)

## Установка зависимостей

Если `tsx` еще не установлен:

```bash
npm install -D tsx
```

## Настройка credentials

### AWS CLI Credentials (Рекомендуется)

Скрипт использует AWS CLI credentials автоматически. Убедитесь, что AWS CLI настроен:

```bash
aws configure
```

Введите:
- AWS Access Key ID
- AWS Secret Access Key
- Default region (например, `us-east-1`)
- Default output format (можно оставить `json`)

Скрипт автоматически использует эти credentials для доступа к DynamoDB.

### Альтернатива: Environment Variables

Можно также использовать environment variables:

```bash
# Windows (PowerShell)
$env:AWS_ACCESS_KEY_ID="your-access-key"
$env:AWS_SECRET_ACCESS_KEY="your-secret-key"
$env:AWS_DEFAULT_REGION="us-east-1"

# Linux/Mac
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_DEFAULT_REGION="us-east-1"
```

## Запуск скрипта

### Способ 1: Через npm script (Рекомендуется)

```bash
npm run seed:books
```

### Способ 2: Напрямую через tsx

```bash
npx tsx scripts/seed-books-cli.ts
```

### Способ 3: Через Node.js (если скрипт скомпилирован)

```bash
node scripts/seed-books-cli.js
```

## Что делает скрипт

1. **Загрузка credentials:**
   - Загружает AWS credentials из AWS CLI конфигурации
   - Инициализирует DynamoDB client с этими credentials
   - Автоматически определяет имя таблицы из Amplify метаданных

2. **Проверка существующих данных:**
   - Проверяет, сколько книг уже существует в таблице
   - Если все 66 книг уже существуют, скрипт завершается

3. **Создание книг:**
   - Итерируется по всем 66 книгам из `scripts/seed-books-data.ts`
   - Для каждой книги:
     - Проверяет, не существует ли уже книга с таким `shortName` (через GSI)
     - Если не существует - создает запись напрямую в DynamoDB через `PutItem`
     - Если существует - пропускает (idempotent)

4. **Отчет:**
   - Выводит статистику: сколько создано, пропущено, ошибок
   - Завершается с кодом 0 при успехе, 1 при ошибках

## Пример вывода

```
🌱 Starting Books table seed...

Authenticating user: admin@example.com...
✅ Authentication successful
✅ User has required role
📚 Found 0 existing books in database

📖 Creating 66 books...

[1/66] ✅ Created: Бытие (OLD)
[2/66] ✅ Created: Исход (OLD)
[3/66] ✅ Created: Левит (OLD)
...
[66/66] ✅ Created: Откровение (NEW)

==================================================
📊 Seed Summary:
   ✅ Created: 66
   ⏭️  Skipped: 0
   ❌ Errors: 0
   📚 Total: 66
==================================================

✅ Seed completed successfully!
```

## Обработка ошибок

### Ошибка загрузки credentials

```
❌ Failed to load AWS credentials!
```

**Решение:** 
- Убедитесь, что AWS CLI настроен: `aws configure`
- Или установите environment variables: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`

### Ошибка прав доступа DynamoDB

```
❌ Error: User is not authorized to perform: dynamodb:PutItem
```

**Решение:** Убедитесь, что IAM user/role имеет права на DynamoDB:
- `dynamodb:PutItem` на таблицу Book
- `dynamodb:Query` на таблицу Book и индекс byShortName
- `dynamodb:Scan` на таблицу Book

### Ошибка сети/AppSync

```
❌ Error: Network error
```

**Решение:** 
- Проверьте, что AppSync API развернут (`amplify status`)
- Проверьте правильность endpoint в `src/amplifyconfiguration.json`
- Проверьте интернет-соединение

### Дубликаты

Скрипт автоматически пропускает уже существующие книги. Если книга уже существует, она будет пропущена:

```
[5/66] ⏭️  Skipped: Бытие (already exists)
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файлы с credentials в git
- Используйте `.env.local` и добавьте его в `.gitignore`
- Используйте отдельного пользователя для seed операций (не production admin)
- После выполнения seed можно удалить credentials из environment

## Troubleshooting

### Проблема: "Cannot find module 'aws-amplify'"

**Решение:** Убедитесь, что зависимости установлены:
```bash
npm install
```

### Проблема: "tsx: command not found"

**Решение:** Установите tsx:
```bash
npm install -D tsx
```

### Проблема: "ResourceNotFoundException" или "Table not found"

**Решение:** 
- Убедитесь, что таблица Book создана через `amplify push`
- Проверьте правильность имени таблицы (скрипт определяет автоматически)
- Можно задать имя таблицы вручную через `BOOK_TABLE_NAME` environment variable

### Проблема: Скрипт создает только часть книг

**Решение:**
- Проверьте логи на наличие ошибок
- Скрипт продолжает работу даже при ошибках отдельных книг
- Повторный запуск безопасен (idempotent) - пропустит уже созданные книги

## Альтернативные способы seed

### Через Server Action (из приложения)

Если у вас есть доступ к приложению, можно использовать Server Action:

```typescript
import { seedBooks } from '@/actions/books';

const result = await seedBooks();
```

### Через AWS AppSync Console

Можно выполнить mutations вручную через AWS AppSync Console:

1. Откройте AWS AppSync Console
2. Перейдите в раздел Queries
3. Выполните mutation `createBook` для каждой книги

### Через AWS CLI (сложнее)

AWS CLI не имеет прямой команды для GraphQL mutations, но можно использовать `aws appsync` с правильной авторизацией (требует дополнительной настройки).

## Связанные файлы

- `scripts/seed-books-cli.ts` - основной скрипт
- `scripts/seed-books-data.ts` - данные всех 66 книг Библии
- `actions/books.ts` - Server Action для seed (альтернативный способ)
- `src/amplifyconfiguration.json` - конфигурация Amplify
- `src/graphql/mutations.ts` - GraphQL mutations

## Дополнительная информация

- [AWS Amplify Documentation](https://docs.amplify.aws/)
- [AppSync GraphQL API](https://docs.aws.amazon.com/appsync/)
- [Cognito User Pools](https://docs.aws.amazon.com/cognito/)

