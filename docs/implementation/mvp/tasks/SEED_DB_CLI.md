# Seed Database Tables via CLI - Инструкция

## Описание

Скрипт для заполнения всех таблиц DynamoDB тестовыми данными через командную строку. Скрипт использует AWS CLI credentials для прямого доступа к DynamoDB, минуя AppSync API.

## Предварительные требования

1. ✅ **AWS CLI настроен** - credentials должны быть настроены через `aws configure`
2. ✅ **IAM permissions** - AWS credentials должны иметь права на DynamoDB (PutItem, Query, Scan на все таблицы)
3. ✅ **DynamoDB таблицы созданы** - все таблицы должны быть созданы через `amplify push`
4. ✅ **Book таблица заполнена** - таблица Book должна быть заполнена через `npm run seed:books`
5. ✅ **Зависимости установлены** - `npm install` должен быть выполнен
6. ✅ **tsx установлен** - для запуска TypeScript скриптов (`npm install -D tsx`)

## Установка зависимостей

Если `tsx` еще не установлен:

```bash
npm install -D tsx
```

## Настройка credentials

### AWS CLI Credentials (Рекомендуется)

Скрипт использует AWS CLI credentials автоматически. Убедитесь, что AWS CLI настроен:

```bash
aws configure
```

Введите:
- AWS Access Key ID
- AWS Secret Access Key
- Default region (например, `us-east-1`)
- Default output format (можно оставить `json`)

Скрипт автоматически использует эти credentials для доступа к DynamoDB.

### Альтернатива: Environment Variables

Можно также использовать environment variables:

```bash
# Windows (PowerShell)
$env:AWS_ACCESS_KEY_ID="your-access-key"
$env:AWS_SECRET_ACCESS_KEY="your-secret-key"
$env:AWS_DEFAULT_REGION="us-east-1"

# Linux/Mac
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_DEFAULT_REGION="us-east-1"
```

## Запуск скрипта

### Способ 1: Через npm script (Рекомендуется)

```bash
npm run seed:db
```

### Способ 2: Напрямую через tsx

```bash
npx tsx scripts/seed-db-cli.ts
```

## Что делает скрипт

1. **Загрузка credentials:**
   - Загружает AWS credentials из AWS CLI конфигурации
   - Инициализирует DynamoDB client с этими credentials
   - Автоматически определяет имена таблиц из Amplify метаданных

2. **Проверка структуры таблиц:**
   - Инспектирует структуру всех таблиц (опционально, для отладки)
   - Получает существующие книги из таблицы Book для использования в GoldenVerse

3. **Создание тестовых данных:**
   - Создает все сущности в правильном порядке с учетом зависимостей
   - Проверяет существование записей перед созданием (idempotent)
   - Создает связи между сущностями

4. **Порядок создания:**
   1. User (13 экземпляров: 3 TEACHER + 2 ADMIN + 8 PARENT)
   2. Grade (3 группы)
   3. UserGrade (3 связи)
   4. AcademicYear (3 экземпляра)
   5. Lesson (15 экземпляров)
   6. GoldenVerse (15 экземпляров, использует существующие книги)
   7. LessonGoldenVerse (30 связей)
   8. Pupil (15 экземпляров)
   9. HomeworkCheck (45 экземпляров, по 3 на каждого ученика)
   10. Achievement (6 экземпляров)
   11. PupilAchievement (12 связей)
   12. Family (5 экземпляров)
   13. FamilyMember (15 связей)
   14. UserFamily (12 связей)
   15. GradeEvent (12 экземпляров)
   16. GradeSettings (3 экземпляра)

5. **Отчет:**
   - Выводит статистику по каждой сущности: сколько создано, пропущено, ошибок
   - Выводит общую статистику в конце
   - Завершается с кодом 0 при успехе, 1 при ошибках

## Количество тестовых данных

Скрипт создает оптимальное количество данных для полноценного тестирования интерфейса и функционала:

- **User**: 13 (3 TEACHER + 2 ADMIN + 8 PARENT)
- **Grade**: 3 группы
- **AcademicYear**: 3 (по 1 на каждую группу)
- **Lesson**: 15 (по 5 на каждую группу)
- **GoldenVerse**: 15
- **Pupil**: 15 (по 5 на каждую группу)
- **HomeworkCheck**: 45 (по 3 на каждого ученика)
- **Achievement**: 6
- **Family**: 5
- **GradeEvent**: 12 (по 4 на каждую группу)
- **GradeSettings**: 3 (по 1 на каждую группу)
- **Связующие сущности**: 72 связи

**Итого**: ~180 записей + 72 связи = **~252 записи**

## Пример вывода

```
🌱 Starting database seed using AWS CLI credentials...

✅ AWS credentials loaded from AWS CLI
   Region: us-east-1

📋 Inspecting table structures...

   Table: User-2ito3uqzjbdcbonnabmm3io6x4-dev
   Key Schema: [{"AttributeName":"id","KeyType":"HASH"}]
   ...

👥 Creating 13 users...

[1/13] ✅ Created: Иванова Мария Васильевна (TEACHER)
[2/13] ✅ Created: Петров Иван Сергеевич (TEACHER)
...

📚 Creating 3 grades...

[1/3] ✅ Created: Младшая группа
[2/3] ✅ Created: Средняя группа
...

============================================================
📊 Seed Summary:
============================================================

Users:
   ✅ Created: 13
   ⏭️  Skipped: 0
   ❌ Errors: 0

Grades:
   ✅ Created: 3
   ⏭️  Skipped: 0
   ❌ Errors: 0

...

============================================================
📈 Totals:
   ✅ Created: 252
   ⏭️  Skipped: 0
   ❌ Errors: 0
============================================================

✅ Seed completed successfully!
```

## Обработка ошибок

### Ошибка загрузки credentials

```
❌ Failed to load AWS credentials!
```

**Решение:** 
- Убедитесь, что AWS CLI настроен: `aws configure`
- Или установите environment variables: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`

### Ошибка прав доступа DynamoDB

```
❌ Error: User is not authorized to perform: dynamodb:PutItem
```

**Решение:** Убедитесь, что IAM user/role имеет права на DynamoDB:
- `dynamodb:PutItem` на все таблицы
- `dynamodb:Query` на все таблицы и индексы
- `dynamodb:Scan` на все таблицы
- `dynamodb:DescribeTable` на все таблицы

### Ошибка отсутствия книг

```
❌ No books found in database. Please run `npm run seed:books` first.
```

**Решение:** 
- Запустите `npm run seed:books` для заполнения таблицы Book
- Убедитесь, что таблица Book существует и содержит данные

### Ошибка отсутствия таблиц

```
❌ Error: ResourceNotFoundException: Requested resource not found
```

**Решение:** 
- Убедитесь, что все таблицы созданы через `amplify push`
- Проверьте, что Amplify метаданные доступны в `amplify/backend/amplify-meta.json`
- Или установите переменные окружения для имен таблиц (например, `USER_TABLE_NAME`)

### Дубликаты

Скрипт автоматически пропускает уже существующие записи. Если запись уже существует, она будет пропущена:

```
[5/13] ⏭️  Skipped: Иванова Мария Васильевна (already exists)
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файлы с credentials в git
- Используйте `.env.local` и добавьте его в `.gitignore`
- Используйте отдельного пользователя для seed операций (не production admin)
- Тестовые данные не должны содержать реальные персональные данные

## Использование тестовых данных

После выполнения скрипта вы получите полноценный набор тестовых данных для:

- ✅ Тестирования таблиц и списков (пагинация, фильтрация, сортировка)
- ✅ Тестирования проверки домашних заданий
- ✅ Тестирования рейтинга учеников
- ✅ Тестирования истории учеников
- ✅ Тестирования расписания (календарь)
- ✅ Тестирования семейных связей
- ✅ Тестирования всех интерфейсов приложения

## Очистка данных

Для удаления всех тестовых данных используйте AWS Console или AWS CLI:

```bash
# Пример удаления всех записей из таблицы (осторожно!)
aws dynamodb scan --table-name User-<apiId>-<env> --attributes-to-get id | \
  jq -r '.Items[].id.S' | \
  xargs -I {} aws dynamodb delete-item --table-name User-<apiId>-<env> --key '{"id":{"S":"{}"}}'
```

⚠️ **Внимание:** Будьте осторожны при удалении данных. Рекомендуется использовать отдельное окружение для тестирования.

## Troubleshooting

### Проблема: Скрипт не может найти таблицы

**Решение:**
1. Проверьте, что `amplify/backend/amplify-meta.json` существует
2. Проверьте, что `apiId` доступен в метаданных
3. Установите переменные окружения для имен таблиц вручную

### Проблема: Ошибки при создании связей

**Решение:**
1. Убедитесь, что все базовые сущности созданы успешно
2. Проверьте, что ID в связях соответствуют существующим записям
3. Проверьте логи для определения конкретной проблемы

### Проблема: Неправильные bookId в GoldenVerse

**Решение:**
1. Убедитесь, что таблица Book заполнена (`npm run seed:books`)
2. Скрипт использует простую логику сопоставления по названию книги
3. При необходимости можно вручную исправить bookId в `seed-db-data.ts`

## Дополнительная информация

- См. [`scripts/seed-db-data.ts`](../../../../scripts/seed-db-data.ts) - данные для всех сущностей
- См. [`scripts/seed-db-cli.ts`](../../../../scripts/seed-db-cli.ts) - основной скрипт
- См. [`docs/database/GRAPHQL_SCHEMA.md`](../../../database/GRAPHQL_SCHEMA.md) - GraphQL схема
- См. [`SEED_BOOKS_CLI.md`](./SEED_BOOKS_CLI.md) - инструкция по заполнению таблицы Book

