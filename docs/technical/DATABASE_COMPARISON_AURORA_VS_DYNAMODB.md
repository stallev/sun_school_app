# Сравнительный анализ: AWS Aurora vs AWS DynamoDB для Sunday School App

## Версия документа: 1.0
**Дата создания:** 11 ноября 2025  
**Проект:** Sunday School App (Приложение для управления воскресной школой)  
**Автор:** AI Senior Database Architect & AWS Solutions Architect

---

## 📋 Executive Summary

**Рекомендация:** **AWS DynamoDB** с гибридным подходом (DynamoDB основной + RDS Aurora опционально для сложных аналитических запросов)

**Обоснование:**
- Проект имеет четко определенные паттерны доступа к данным
- Большинство запросов простые (CRUD операции)
- Требуется автоматическое масштабирование
- Интеграция с AWS Amplify и AppSync упрощает разработку
- Стоимость DynamoDB предсказуема и может быть ниже для MVP
- Возможность добавления Aurora позже для сложных запросов

---

## 1. Анализ требований проекта

### 1.1. Структура данных (из ERD.md)

**Основные сущности:**
- User (пользователи) - хранится в Cognito + DynamoDB для доп. атрибутов
- Grade (группы) - ~10-20 записей
- AcademicYear (учебные годы) - ~5-10 на группу
- Lesson (уроки) - ~30-50 на учебный год
- Pupil (ученики) - ~10-30 на группу
- HomeworkCheck (проверки ДЗ) - ~300-1500 записей (урок × ученик)
- GoldenVerse (стихи) - ~100-500 записей
- Achievement (достижения) - ~10-20 записей
- Family (семьи) - ~20-50 записей

**Оценка объема данных (MVP):**
- Общее количество записей: ~5,000-10,000
- Размер данных: ~50-100 MB
- Рост данных: ~1,000-2,000 записей в год

### 1.2. Паттерны доступа к данным

#### 1.2.1. Частые операции (80% запросов)

1. **Получение уроков группы за учебный год**
   - Паттерн: `WHERE gradeId = X AND academicYearId = Y ORDER BY date DESC`
   - Частота: Высокая (каждый раз при открытии страницы группы)
   - Сложность: Простая (фильтрация + сортировка)

2. **Получение проверок ДЗ для урока**
   - Паттерн: `WHERE lessonId = X`
   - Частота: Высокая (при проверке ДЗ)
   - Сложность: Простая (прямой доступ по ID)

3. **Получение учеников группы**
   - Паттерн: `WHERE gradeId = X AND active = true`
   - Частота: Высокая
   - Сложность: Простая

4. **Получение рейтинга группы**
   - Паттерн: `SUM(points) GROUP BY pupilId WHERE academicYearId = X ORDER BY total DESC`
   - Частота: Средняя
   - Сложность: Средняя (агрегация)

5. **Получение истории ученика**
   - Паттерн: `WHERE pupilId = X ORDER BY date DESC`
   - Частота: Средняя
   - Сложность: Простая

#### 1.2.2. Сложные операции (20% запросов)

1. **Статистика по золотым стихам**
   - Паттерн: `JOIN LessonGoldenVerse + Lesson + COUNT GROUP BY verseId`
   - Частота: Низкая
   - Сложность: Высокая (множественные JOIN)

2. **Аналитика по группам**
   - Паттерн: `JOIN Grade + AcademicYear + Lesson + HomeworkCheck + агрегация`
   - Частота: Низкая
   - Сложность: Высокая (сложные JOIN)

3. **Поиск по множественным критериям**
   - Паттерн: `WHERE name LIKE '%X%' AND gradeId = Y AND active = true`
   - Частота: Средняя
   - Сложность: Средняя

### 1.3. Требования к производительности

- **Время отклика:** < 200ms для простых запросов
- **Пропускная способность:** 100-500 запросов/сек (пиковая нагрузка)
- **Масштабируемость:** Автоматическое масштабирование при росте данных
- **Доступность:** 99.9% uptime

### 1.4. Требования к целостности данных

- **Foreign Keys:** Требуются для связей между сущностями
- **Транзакции:** Требуются для массовых операций (проверка ДЗ для всех учеников)
- **Уникальные ограничения:** Email, комбинации полей
- **Каскадные операции:** При удалении родительской записи удалять дочерние

---

## 2. Сравнительный анализ: AWS Aurora vs DynamoDB

### 2.1. AWS Aurora (Serverless PostgreSQL)

#### 2.1.1. Преимущества

✅ **Реляционная модель данных**
- Поддержка SQL и сложных JOIN запросов
- Foreign Keys и транзакции из коробки
- Нормализация данных (3NF)
- ACID транзакции

✅ **Гибкость запросов**
- Сложные аналитические запросы
- Полнотекстовый поиск
- Агрегации и GROUP BY
- Подзапросы и CTE

✅ **Совместимость с существующим кодом**
- Если бы использовался Prisma, миграция была бы проще
- Стандартный SQL синтаксис
- Поддержка ORM (TypeORM, Sequelize)

✅ **Целостность данных**
- Foreign Keys с каскадными операциями
- Транзакции для атомарности
- Constraints и валидация на уровне БД

✅ **Автоматическое масштабирование**
- Aurora Serverless v2 автоматически масштабируется
- Read replicas для распределения нагрузки чтения
- Автоматические бэкапы

#### 2.1.2. Недостатки

❌ **Сложность настройки**
- Требуется настройка VPC, Security Groups
- Connection pooling нужно настраивать отдельно
- Более сложная интеграция с Amplify

❌ **Стоимость**
- Минимум ~$50-100/месяц даже для малых нагрузок
- Оплата за ACU (Aurora Capacity Units) даже когда не используется
- Дополнительные затраты на storage и I/O

❌ **Cold starts**
- Aurora Serverless может иметь задержки при первом запросе
- Требуется поддержание минимального количества ACU

❌ **Масштабирование записи**
- Один writer instance (ограничение на запись)
- Масштабирование записи сложнее чем чтения

#### 2.1.3. Стоимость (примерная оценка для MVP)

**Aurora Serverless v2:**
- Минимум: 0.5 ACU = ~$43/месяц
- Storage: $0.10/GB = ~$5-10/месяц (50-100 GB)
- I/O: $0.20/млн запросов = ~$10-20/месяц
- **Итого: ~$60-80/месяц**

**Aurora Provisioned (если не Serverless):**
- db.t4g.medium: ~$60/месяц
- Storage: ~$10/месяц
- **Итого: ~$70/месяц**

### 2.2. AWS DynamoDB

#### 2.2.1. Преимущества

✅ **Простота использования**
- Полная интеграция с AWS Amplify и AppSync
- Автоматическое создание таблиц через GraphQL schema
- Нет необходимости в управлении серверами

✅ **Автоматическое масштабирование**
- On-Demand billing mode - платишь только за использование
- Автоматическое масштабирование до любого объема
- Нет проблем с connection pooling

✅ **Производительность**
- Предсказуемая задержка < 10ms для простых запросов
- Масштабирование чтения и записи независимо
- Глобальные таблицы для мультирегионности (Post-MVP)

✅ **Стоимость**
- On-Demand: $1.25/млн запросов записи, $0.25/млн запросов чтения
- Storage: $0.25/GB
- Для MVP: ~$10-30/месяц

✅ **Интеграция с AWS**
- Нативная интеграция с Lambda, AppSync, Cognito
- Streams для real-time обновлений
- Point-in-time recovery

#### 2.2.2. Недостатки

❌ **Ограничения NoSQL**
- Нет JOIN операций (нужно делать в коде или через несколько запросов)
- Нет Foreign Keys (валидация в коде)
- Ограниченные возможности для сложных запросов
- Нет транзакций между таблицами (только single-item транзакции)

❌ **Сложность моделирования**
- Требуется денормализация данных
- Нужно проектировать таблицы под паттерны доступа
- GSI (Global Secondary Indexes) увеличивают стоимость

❌ **Ограничения запросов**
- Нет полнотекстового поиска (нужен Elasticsearch)
- Сложные аналитические запросы требуют дополнительных сервисов
- Ограниченные возможности сортировки и фильтрации

❌ **Кривая обучения**
- Разработчики должны понимать DynamoDB паттерны
- Неправильное проектирование может привести к высокой стоимости

#### 2.2.3. Стоимость (примерная оценка для MVP)

**DynamoDB On-Demand:**
- Запись: 100K запросов/месяц × $1.25/млн = $0.13
- Чтение: 500K запросов/месяц × $0.25/млн = $0.13
- Storage: 50 GB × $0.25/GB = $12.50
- GSI (3 индекса): ~$5-10/месяц
- **Итого: ~$15-25/месяц**

**DynamoDB Provisioned (альтернатива):**
- 5 RCU × $0.00013/час = ~$5/месяц
- 5 WCU × $0.00065/час = ~$11/месяц
- Storage: ~$12.50/месяц
- **Итого: ~$30/месяц**

---

## 3. Анализ применимости к проекту

### 3.1. Структура данных проекта

#### 3.1.1. Реляционные связи

**Связи в проекте:**
- User ↔ Grade (many-to-many через UserGrade)
- Grade → AcademicYear (one-to-many)
- AcademicYear → Lesson (one-to-many)
- Lesson ↔ GoldenVerse (many-to-many через LessonGoldenVerse)
- Lesson → HomeworkCheck (one-to-many)
- Pupil → HomeworkCheck (one-to-many)
- Pupil ↔ Family (many-to-many через FamilyMember)
- Pupil ↔ Achievement (many-to-many через PupilAchievement)

**Анализ:**
- ✅ Много связей many-to-many - **лучше для реляционной БД**
- ✅ Foreign Keys важны для целостности данных - **лучше для Aurora**
- ⚠️ Но большинство запросов простые - **можно реализовать в DynamoDB**

#### 3.1.2. Паттерны доступа

**Анализ частых операций:**

1. **Получение уроков группы** (80% запросов)
   - **Aurora:** `SELECT * FROM lessons WHERE academicYearId = ? ORDER BY date DESC`
   - **DynamoDB:** GSI на `academicYearId`, сортировка по `date`
   - **Вывод:** ✅ Оба варианта работают хорошо

2. **Получение проверок ДЗ** (70% запросов)
   - **Aurora:** `SELECT * FROM homework_checks WHERE lessonId = ?`
   - **DynamoDB:** Прямой доступ по Partition Key `lessonId`
   - **Вывод:** ✅ DynamoDB быстрее для этого паттерна

3. **Рейтинг группы** (30% запросов)
   - **Aurora:** `SELECT pupilId, SUM(points) FROM homework_checks WHERE ... GROUP BY pupilId ORDER BY SUM(points) DESC`
   - **DynamoDB:** Требуется агрегация в коде или использование DynamoDB Streams + Lambda
   - **Вывод:** ⚠️ Aurora проще для агрегаций

4. **Статистика по стихам** (10% запросов)
   - **Aurora:** Сложный JOIN запрос
   - **DynamoDB:** Несколько запросов + агрегация в коде
   - **Вывод:** ⚠️ Aurora проще, но редко используется

### 3.2. Требования к транзакциям

**Сценарии использования транзакций:**

1. **Массовая проверка ДЗ**
   - Создание/обновление множества записей HomeworkCheck атомарно
   - **Aurora:** Полная поддержка транзакций
   - **DynamoDB:** TransactWriteItems (до 25 items, только в одной таблице)

2. **Создание урока с золотыми стихами**
   - Создание Lesson + множества LessonGoldenVerse атомарно
   - **Aurora:** Транзакция между таблицами
   - **DynamoDB:** TransactWriteItems (если все в одной таблице) или условная логика

3. **Завершение учебного года**
   - Обновление статуса AcademicYear + создание нового года атомарно
   - **Aurora:** Транзакция
   - **DynamoDB:** TransactWriteItems

**Вывод:** 
- ⚠️ Aurora лучше для сложных транзакций между таблицами
- ✅ DynamoDB достаточно для большинства сценариев проекта

### 3.3. Масштабируемость

**Текущий масштаб (MVP):**
- ~5,000-10,000 записей
- ~100-500 запросов/сек
- Рост ~1,000-2,000 записей/год

**Будущий масштаб (Post-MVP):**
- Возможный рост до 50,000-100,000 записей
- Возможный рост до 1,000-2,000 запросов/сек
- Множественные церкви/школы

**Анализ:**
- ✅ **DynamoDB:** Автоматически масштабируется без изменений в коде
- ⚠️ **Aurora:** Требуется настройка read replicas и масштабирование writer instance

**Вывод:** ✅ DynamoDB лучше для автоматического масштабирования

### 3.4. Интеграция с AWS Amplify

**AWS Amplify Data:**
- ✅ Нативная поддержка DynamoDB через AppSync
- ✅ Автоматическое создание таблиц из GraphQL schema
- ✅ Автоматическая генерация TypeScript типов
- ⚠️ Aurora требует настройки через Lambda resolvers или RDS Data API

**Вывод:** ✅ DynamoDB имеет лучшую интеграцию с Amplify

---

## 4. Рекомендации по проектированию для DynamoDB

### 4.1. Стратегия моделирования данных

#### 4.1.1. Single Table Design (рекомендуется)

**Подход:** Одна таблица для связанных сущностей с использованием составных ключей

**Пример структуры:**

```typescript
// Таблица: SundaySchoolData
// Partition Key (PK): entityType#entityId (например, "LESSON#clxxx123")
// Sort Key (SK): relatedEntityType#relatedEntityId (например, "GOLDEN_VERSE#clxxx456")
// GSI1PK: gradeId (для запросов по группе)
// GSI1SK: date (для сортировки)

// Примеры записей:
{
  PK: "LESSON#clxxx123",
  SK: "METADATA",
  entityType: "LESSON",
  title: "Любовь к ближнему",
  academicYearId: "clxxx789",
  gradeId: "clxxx456",
  date: "2024-11-10",
  // ... другие поля
}

{
  PK: "LESSON#clxxx123",
  SK: "GOLDEN_VERSE#clxxx111",
  entityType: "LESSON_GOLDEN_VERSE",
  lessonId: "clxxx123",
  goldenVerseId: "clxxx111",
  order: 1
}

{
  PK: "HOMEWORK_CHECK#clxxx123",
  SK: "PUPIL#clxxx999",
  entityType: "HOMEWORK_CHECK",
  lessonId: "clxxx123",
  pupilId: "clxxx999",
  points: 30,
  // ... другие поля
}
```

**Преимущества:**
- Минимум запросов для получения связанных данных
- Эффективное использование DynamoDB capacity
- Проще реализовать транзакции

**Недостатки:**
- Более сложная структура данных
- Требуется больше времени на проектирование

#### 4.1.2. Multi-Table Design (альтернатива)

**Подход:** Отдельная таблица для каждой сущности (как в реляционной БД)

**Пример структуры:**

```typescript
// Таблица: Lessons
// PK: id
// GSI1: academicYearId-date (для запросов по учебному году)

// Таблица: HomeworkChecks
// PK: lessonId
// SK: pupilId
// GSI1: pupilId-date (для истории ученика)

// Таблица: Pupils
// PK: id
// GSI1: gradeId-active (для списка учеников группы)
```

**Преимущества:**
- Проще понять структуру (похоже на реляционную БД)
- Легче мигрировать с реляционной модели

**Недостатки:**
- Больше запросов для получения связанных данных
- Выше стоимость (больше GSI)

**Рекомендация:** Использовать **Multi-Table Design** для MVP (проще начать), затем оптимизировать до Single Table Design при необходимости.

### 4.2. Реализация связей в DynamoDB

#### 4.2.1. One-to-Many связи

**Пример: AcademicYear → Lessons**

```typescript
// Таблица: Lessons
// PK: id
// GSI1PK: academicYearId
// GSI1SK: date

// Запрос: Получить все уроки учебного года
const { data } = await amplifyData.graphql({
  query: queries.listLessons,
  variables: {
    filter: { academicYearId: { eq: yearId } },
  },
});
```

#### 4.2.2. Many-to-Many связи

**Пример: Lesson ↔ GoldenVerse**

**Вариант 1: Через промежуточную таблицу**
```typescript
// Таблица: LessonGoldenVerses
// PK: lessonId
// SK: order#goldenVerseId

// Получение стихов урока:
// Query по PK = lessonId, сортировка по SK
```

**Вариант 2: Денормализация**
```typescript
// В таблице Lessons хранить массив goldenVerseIds
// lesson.goldenVerseIds = ["clxxx111", "clxxx222", "clxxx333"]

// Получение стихов:
// 1. Получить урок (с массивом IDs)
// 2. BatchGet для получения стихов по IDs
```

**Рекомендация:** Использовать **Вариант 1** для целостности данных, **Вариант 2** для производительности (если стихи редко изменяются).

#### 4.2.3. Foreign Keys и валидация

**В DynamoDB нет Foreign Keys**, поэтому валидация должна быть в коде:

```typescript
// Server Action: createLesson
export async function createLesson(data: CreateLessonInput) {
  // 1. Валидация существования academicYearId
  const year = await getAcademicYearById(data.academicYearId);
  if (!year) {
    return { success: false, error: 'Учебный год не найден' };
  }
  
  // 2. Валидация существования goldenVerseIds
  for (const verseId of data.goldenVerseIds) {
    const verse = await getGoldenVerseById(verseId);
    if (!verse) {
      return { success: false, error: `Стих ${verseId} не найден` };
    }
  }
  
  // 3. Создание урока
  const lesson = await createLessonMutation(data);
  
  return { success: true, data: lesson };
}
```

### 4.3. Реализация сложных запросов

#### 4.3.1. Рейтинг группы (агрегация)

**Проблема:** DynamoDB не поддерживает GROUP BY и агрегации

**Решение 1: Агрегация в коде**
```typescript
// 1. Получить все проверки ДЗ для учебного года
const checks = await getHomeworkChecksByAcademicYear(yearId);

// 2. Агрегация в коде
const leaderboard = checks.reduce((acc, check) => {
  if (!acc[check.pupilId]) {
    acc[check.pupilId] = { pupilId: check.pupilId, totalPoints: 0 };
  }
  acc[check.pupilId].totalPoints += check.points;
  return acc;
}, {});

// 3. Сортировка
const sorted = Object.values(leaderboard).sort((a, b) => b.totalPoints - a.totalPoints);
```

**Решение 2: Pre-computed агрегация (рекомендуется)**
```typescript
// Таблица: PupilLeaderboard
// PK: academicYearId
// SK: totalPoints#pupilId (для сортировки)
// GSI1: gradeId-totalPoints

// Обновление при изменении проверки ДЗ:
// 1. Обновить HomeworkCheck
// 2. Пересчитать totalPoints для pupilId
// 3. Обновить PupilLeaderboard запись
```

**Рекомендация:** Использовать **Решение 2** для производительности (особенно при большом количестве данных).

#### 4.3.2. Статистика по золотым стихам

**Проблема:** Требуется JOIN между LessonGoldenVerse, Lesson, и агрегация

**Решение:**
```typescript
// Таблица: GoldenVerseStats
// PK: goldenVerseId
// SK: academicYearId
// usageCount: количество использований

// Обновление при создании урока:
// 1. Создать Lesson
// 2. Создать LessonGoldenVerse записи
// 3. Increment usageCount в GoldenVerseStats для каждого стиха
```

### 4.4. Транзакции в DynamoDB

**Ограничения:**
- TransactWriteItems поддерживает до 25 items
- Все items должны быть в одной таблице (или использовать условную логику)
- Нет поддержки транзакций между разными таблицами

**Пример: Массовая проверка ДЗ**
```typescript
// Все HomeworkCheck записи в одной таблице
const items = pupils.map(pupil => ({
  Put: {
    TableName: 'HomeworkChecks',
    Item: {
      lessonId: lesson.id,
      pupilId: pupil.id,
      points: calculatedPoints,
      // ...
    },
  },
}));

await dynamoDBClient.send(
  new TransactWriteCommand({
    TransactItems: items.slice(0, 25), // Максимум 25 items
  })
);
```

**Для > 25 items:** Разбить на батчи по 25 items.

---

## 5. Сравнительная таблица

| Критерий | AWS Aurora | AWS DynamoDB | Победитель |
|----------|------------|--------------|------------|
| **Простота использования** | ⚠️ Средняя (требует настройки) | ✅ Высокая (интеграция с Amplify) | DynamoDB |
| **Производительность простых запросов** | ✅ Хорошая (< 50ms) | ✅ Отличная (< 10ms) | DynamoDB |
| **Сложные JOIN запросы** | ✅ Полная поддержка | ❌ Не поддерживается | Aurora |
| **Транзакции** | ✅ Полная поддержка | ⚠️ Ограниченная (25 items, одна таблица) | Aurora |
| **Масштабирование** | ⚠️ Требует настройки | ✅ Автоматическое | DynamoDB |
| **Стоимость (MVP)** | ⚠️ ~$60-80/месяц | ✅ ~$15-25/месяц | DynamoDB |
| **Целостность данных** | ✅ Foreign Keys, Constraints | ⚠️ Валидация в коде | Aurora |
| **Интеграция с Amplify** | ⚠️ Требует настройки | ✅ Нативная поддержка | DynamoDB |
| **Кривая обучения** | ✅ Низкая (SQL) | ⚠️ Средняя (NoSQL паттерны) | Aurora |
| **Гибкость запросов** | ✅ Высокая (SQL) | ⚠️ Ограниченная (заранее спроектированные запросы) | Aurora |
| **Backup и восстановление** | ✅ Automated backups | ✅ Point-in-time recovery | Ничья |
| **Мониторинг** | ✅ CloudWatch + Performance Insights | ✅ CloudWatch Metrics | Ничья |

---

## 6. Рекомендация для проекта

### 6.1. Основная рекомендация: **AWS DynamoDB**

**Обоснование:**

1. **80% запросов простые** - DynamoDB отлично подходит
2. **Интеграция с Amplify** - упрощает разработку
3. **Стоимость** - в 3-4 раза дешевле для MVP
4. **Масштабирование** - автоматическое без изменений в коде
5. **Производительность** - предсказуемая задержка < 10ms

### 6.2. Гибридный подход (рекомендуется для Post-MVP)

**Стратегия:**
- **DynamoDB** - основная БД для всех операций
- **AWS Aurora** (опционально) - для сложных аналитических запросов (если понадобится)

**Когда использовать Aurora:**
- Сложная аналитика по всем группам
- Сложные отчеты с множественными JOIN
- Полнотекстовый поиск по урокам/стихам
- Сложные статистические запросы

**Реализация:**
- Использовать AWS DMS (Database Migration Service) для синхронизации данных из DynamoDB в Aurora
- Или использовать Lambda для записи в обе БД (write-through pattern)

### 6.3. План миграции на DynamoDB

#### Этап 1: Проектирование схемы (1-2 дня)
1. Анализ всех паттернов доступа к данным
2. Проектирование таблиц и GSI
3. Определение структуры Partition Key и Sort Key
4. Планирование денормализации где необходимо

#### Этап 2: Создание GraphQL Schema (1 день)
1. Создание `amplify/backend/api/schema.graphql`
2. Определение типов и связей
3. Настройка `@auth` директив
4. Компиляция: `amplify api gql-compile`

#### Этап 3: Реализация запросов (3-5 дней)
1. Создание GraphQL queries для чтения данных
2. Создание GraphQL mutations для записи данных
3. Реализация валидации Foreign Keys в коде
4. Реализация агрегаций (рейтинг, статистика)

#### Этап 4: Миграция Server Actions (5-7 дней)
1. Обновление всех Server Actions для использования Amplify Data
2. Замена Prisma запросов на GraphQL queries/mutations
3. Реализация транзакций через TransactWriteItems
4. Тестирование всех операций

#### Этап 5: Оптимизация (2-3 дня)
1. Анализ производительности запросов
2. Оптимизация GSI
3. Реализация pre-computed агрегаций где необходимо
4. Настройка кеширования

**Общая оценка времени:** 12-18 дней

---

## 7. Риски и митигация

### 7.1. Риски DynamoDB

#### Риск 1: Неправильное проектирование таблиц
**Влияние:** Высокая стоимость, плохая производительность  
**Митигация:**
- Тщательное проектирование на основе паттернов доступа
- Использование AWS Well-Architected Framework для DynamoDB
- Регулярный review структуры таблиц
- Мониторинг стоимости через CloudWatch

#### Риск 2: Ограничения сложных запросов
**Влияние:** Невозможность выполнить некоторые аналитические запросы  
**Митигация:**
- Pre-computed агрегации для частых запросов
- Использование DynamoDB Streams + Lambda для обновления агрегаций
- Возможность добавления Aurora позже для аналитики

#### Риск 3: Отсутствие Foreign Keys
**Влияние:** Возможность создания "осиротевших" записей  
**Митидация:**
- Валидация в Server Actions перед созданием/обновлением
- Использование условных выражений (ConditionExpression) в DynamoDB
- Регулярные проверки целостности данных (Lambda функции)

### 7.2. Риски Aurora

#### Риск 1: Высокая стоимость для малых нагрузок
**Влияние:** Переплата за неиспользуемые ресурсы  
**Митигация:**
- Использование Aurora Serverless v2
- Настройка минимального ACU на низком уровне
- Мониторинг использования и оптимизация

#### Риск 2: Сложность интеграции с Amplify
**Влияние:** Больше времени на настройку и разработку  
**Митигация:**
- Использование RDS Data API для упрощения доступа
- Lambda resolvers для AppSync
- Документация и примеры кода

#### Риск 3: Необходимость управления соединениями
**Влияние:** Проблемы с connection pooling  
**Митигация:**
- Использование RDS Proxy для connection pooling
- Правильная настройка Lambda concurrency
- Мониторинг количества соединений

---

## 8. Итоговые выводы и рекомендации

### 8.1. Для MVP: **AWS DynamoDB**

**Причины:**
1. ✅ **80% запросов простые** - DynamoDB идеально подходит
2. ✅ **Интеграция с Amplify** - быстрее разработка
3. ✅ **Стоимость** - в 3-4 раза дешевле
4. ✅ **Масштабирование** - автоматическое
5. ✅ **Производительность** - предсказуемая и быстрая

**Компромиссы:**
- ⚠️ Сложные аналитические запросы потребуют дополнительной работы
- ⚠️ Валидация Foreign Keys в коде (но это управляемо)
- ⚠️ Ограничения транзакций (но достаточно для проекта)

### 8.2. Для Post-MVP: Рассмотреть гибридный подход

**Если потребуется:**
- Сложная аналитика по всем группам
- Сложные отчеты с JOIN
- Полнотекстовый поиск

**Тогда добавить:**
- AWS Aurora для аналитических запросов
- AWS DMS для синхронизации данных
- Или использовать AWS Athena для query данных из DynamoDB

### 8.3. Финальная рекомендация

**Использовать AWS DynamoDB как основную БД** с возможностью добавления Aurora позже при необходимости.

**План действий:**
1. ✅ Проектирование DynamoDB схемы на основе ERD.md
2. ✅ Создание GraphQL schema в Amplify
3. ✅ Реализация всех операций через GraphQL
4. ✅ Мониторинг производительности и стоимости
5. ⚠️ При необходимости добавить Aurora для аналитики

---

## 9. Дополнительные ресурсы

### 9.1. AWS Документация
- [DynamoDB Best Practices](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/best-practices.html)
- [DynamoDB Design Patterns](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-general-nosql-design.html)
- [Aurora Serverless v2](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2.html)
- [AWS Amplify Data](https://docs.amplify.aws/react/build-a-backend/data/)

### 9.2. Книги и статьи
- "The DynamoDB Book" by Alex DeBrie
- "Designing Data-Intensive Applications" by Martin Kleppmann (глава о NoSQL)

### 9.3. Инструменты
- [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html) - для локальной разработки
- [NoSQL Workbench](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/workbench.html) - для визуализации данных

---

**Последнее обновление:** 11 ноября 2025  
**Версия документа:** 1.0  
**Автор:** AI Senior Database Architect & AWS Solutions Architect
